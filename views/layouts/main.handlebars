<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Lock it Up!</title>
    <link rel='stylesheet' href='/css/jass.css' />
    <link rel='stylesheet' href='/css/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  </head>

  <body>
       <nav class="navbar navbar-expand-lg navbar-dark  bg-dark">
      <div class="container-fluid">
        <h1>
          <a class="navbar-brand" href="/">
          <span role='img' aria-label='lock'>🔐</span></a>

        </h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          
          <ul class="navbar-nav ms-auto mb-2 mb-lg-1">
            {{#if logged_in}}
          Welcome
          {{user_name}}

          <li class="nav-item">
              <a class="nav-link " aria-current="page" >{{user_name}}</a>
            </li>
          |
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/view_locks">View Locks</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/add_lock">Add Lock</a>
            </li>
            <li class="nav-item">
              <a class='nav-link' id='logout'>Logout</a>
               </li>
            <li class="nav-item ">
            {{else}}
            <a class='nav-link' href='/login'>login</a>
           {{/if}}
            </li>
          </ul>
        </div>
      </div>
    </nav>
      
      <main class='container container-fluid mt-5'>
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src='/js/logout.js'></script>
      <script src='/js/profile.js'></script>
    {{/if}}
  </body>

</html>